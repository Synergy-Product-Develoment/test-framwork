base.url=http://localhost:8081
timeout.ms=15000
log.request=true
log.response=true
api.version=v1
api.version.strategy=PATH

# DB (optional)
db.enabled=false
db.url=
db.username=
db.password=
db.driver=

# Idempotency
idempotency.enabled=false
idempotency.header=Idempotency-Key

auth.email=krshubh1995@gmail.com
auth.password=123456
auth.deviceKey=QA-AUTOMATION-DEVICE-001
# Retry
retry.enabled=true
retry.maxAttempts=3
retry.backoffMs=1000

# Parallel execution
test.parallel=false
test.threadCount=5
